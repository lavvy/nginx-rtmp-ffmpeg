machine:
  environment:
    IMAGE: dbindloss/nginx-rtmp-ffmpeg

  services:
    - docker

dependencies:
  override:
    - ./build.sh

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASSWORD
      - docker push $IMAGE
